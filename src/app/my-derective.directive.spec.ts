import { MyDerectiveDirective } from './my-derective.directive';

describe('MyDerectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDerectiveDirective();
    expect(directive).toBeTruthy();
  });
});
